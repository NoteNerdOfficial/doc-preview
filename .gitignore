.DS_Store
.claude/
node_modules/
data.json
*.js.map

# Runtime-generated PDF cache — physically lives here once symlinked into a
# vault's .obsidian/plugins/, not source.
preview-cache/

# Copied from node_modules by esbuild.config.mjs on every build.
pdf.worker.min.mjs
